diff --git a/src/name.ts b/src/name.ts
--- a/src/name.ts
+++ b/src/name.ts
@@
-  return value.trim().split(/\s+/).filter(Boolean).map((part) => part.toLowerCase()).join(' ');
+  return value.trim().split(/ +/).filter(Boolean).map((part) => part.toLowerCase()).join(' ');
